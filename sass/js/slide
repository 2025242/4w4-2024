jQuery(document).ready(function($) {
    $('.destinations-carousel').slick({
        slidesToShow: 1,
        slidesToScroll: 1,
        autoplay: true,
        autoplaySpeed: 3000, // Temps de transition entre les diapositives (ms)
        dots: true, // Afficher les points indicateurs de position
        arrows: true // Afficher les flèches de navigation
    });
});